# Generated by HASSL codegen
script:
  hassl_write__sync_living_sync__light_living__onoff__set:
    alias: "HASSL write (sync living_sync \u2192 light.living onoff)"
    mode: single
    sequence:
    - service: input_text.set_value
      data:
        entity_id: input_text.hassl_ctx__light_living
        value: '{{ this.context.id }}'
    - service: <FILL_BY_AUTOMATION>
      target:
        entity_id: light.living
  hassl_write__sync_living_sync__switch_living_circuit__onoff__set:
    alias: "HASSL write (sync living_sync \u2192 switch.living_circuit onoff)"
    mode: single
    sequence:
    - service: input_text.set_value
      data:
        entity_id: input_text.hassl_ctx__switch_living_circuit
        value: '{{ this.context.id }}'
    - service: <FILL_BY_AUTOMATION>
      target:
        entity_id: switch.living_circuit
